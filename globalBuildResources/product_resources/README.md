The `/globalBuildResources/product_resources` directory houses app-specific data.

For use in dev, on `run` the contents of this directory is copied to `/[os]/build/lib/app_resources/product/product_resources`.